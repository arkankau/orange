import { Router } from 'express';
import { Server as SocketIOServer } from 'socket.io';
export default function createRealtimeRouter(io: SocketIOServer): Router;
//# sourceMappingURL=realtime.d.ts.map